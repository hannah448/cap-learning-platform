# Environment
.env
.env.local
.env.*.local

# macOS
.DS_Store

# Node
node_modules/
npm-debug.log*

# Editor
.vscode/
.idea/

# Vercel
.vercel/

# Build artifacts
dist/
build/

# --- Cap Learning / video pipeline ---
# Secrets (JAMAIS commit)
scripts/vimeo_cookies.txt
scripts/.aws-credentials
scripts/*.pem

# --- Cap Learning / Supabase ---
# js/config.js est versionné — la anon key est publique par design
# (la sécurité vient des policies RLS au niveau Postgres).
# Si tu utilises plusieurs environnements (staging/prod), gère plutôt via
# branches Git différentes ou variables Vercel build-time.

# Backups HTML créés par inject-design-system.sh
*.html.bak

# Données locales (trop lourdes / spécifiques à la machine)
# Pattern global : tous les dossiers de vidéos sources/intermédiaires/processed
scripts/videos_*/
scripts/videos_originales/
scripts/videos_afrique_720p/
scripts/videos_afrique_480p/
# Fichiers vidéo isolés
scripts/**/*.mp4
scripts/**/*.mov
scripts/**/*.mkv
scripts/**/*.webm

# Logs runtime
scripts/download_vimeo_log.txt
scripts/traitement_log.txt
# Note : scripts/upload_s3_log.txt est volontairement versionné — il sert
# de source-of-truth pour reproduire/auditer l'upload S3.

# Fichiers de travail (garder les .example versionnés)
scripts/vimeo_urls.txt
scripts/timecodes.csv
